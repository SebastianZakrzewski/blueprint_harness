# Reference project fixture

Inspectable approved docs for the full typescript-node reference stack
(base → nestjs → database → supabase → mastra per OD-007).

Used by M9/M16 gates for temp-dir bootstrap smoke tests. Do not commit
`tmp/` bootstrap output.
